[project]
name = "tests"
version = "0.0.1"
description = "Tests for backend"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=8.4.2",
    "requests>=2.32.5",
    "uuid>=1.30",
]
